Create the complete code for a video player screen for a native Android application using Jetpack Compose with Material 3. The screen must include a colored Box placeholder for the video area, playback controls (play/pause, skip forward, skip back), a slider as an interactive progress bar showing current time and total duration, and icon buttons for fullscreen mode and volume — all controls must remain on the same screen without navigating away. Generate appropriate texts for each element on the screen.