Create the complete code for a notifications screen for a native Android application using Jetpack Compose with Material 3. The screen must display a scrollable list of notifications, each containing a leading icon, a title, a description, and a timestamp; unread notifications must be visually distinct, and tapping a notification must toggle its read/unread state within the same screen. Generate appropriate texts for each element on the screen.